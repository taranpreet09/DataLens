FROM node:22-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci

RUN mkdir -p storage/uploads storage/parsed storage/cache

EXPOSE 5000

CMD ["npx", "nodemon", "--watch", ".", "--ext", "js,json", "--ignore", "node_modules/", "--ignore", "storage/", "server.js"]
